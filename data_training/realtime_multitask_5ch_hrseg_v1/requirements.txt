numpy>=2.1,<3
Pillow>=11,<13
PyYAML>=6,<7
onnx>=1.17,<2
# 1.23+ Windows wheels require the CUDA 13 runtime. This workstation and the
# bundled PyTorch environment use CUDA 12, so keep the teacher-cache runtime on
# the compatible wheel line.
onnxruntime-gpu>=1.20,<1.23
opencv-python-headless>=4.10,<5
tqdm>=4.66,<5
pytest>=8,<10
